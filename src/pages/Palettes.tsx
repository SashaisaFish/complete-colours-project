import React from "react";
import ColourPalette from "../components/ColourPalette";

const Palettes: React.FC = () => {
	return (
		<main>
			{/* 
        fetch all user palettes, receive data response = [{palette}, {palette}, ...] 
        data.map(palette => {
            return <ColourPalette palette={palette} key={palette.name}></ColourPalette>
        })
        */}
		</main>
	);
};

export default Palettes;
